import React from "react";
import ReactDOM from "react-dom/client";
import PawBookApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PawBookApp />
  </React.StrictMode>
);
